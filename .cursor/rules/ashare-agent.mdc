---
description: AShareAgent 本地 A 股分析（仅用 Cursor 作 AI）
globs:
alwaysApply: true
---

# AShareAgent 工作流

本项目 **禁止** 调用 OpenAI / DeepSeek / 硅基流动等外部大模型 API。  
唯一 AI 后端就是当前 Cursor Agent。

## 用户说「分析 XXXXXX」或「分析某股票」时

1. 在项目根执行：
   `E:\TradingAgents-CN\src\venv\Scripts\python.exe -m ashare_agent prepare <代码>`
2. 读取 `data/<代码>_context.md`
3. 按 `ashare_agent/roles.py` 的 6 个角色输出中文报告
4. 用 `ashare_agent.fetch.save_report` 或直接写入 `reports/<代码>_latest.md`
5. 报告末尾加免责声明：研究学习用途，不构成投资建议

## 硬约束

- 不编造财报/新闻；数据包没有的要写「数据缺失」
- A 股社媒只用雪球关注/东财股吧等公开指标
- 不要引导用户去部署 TradingAgents-CN 或申请第三方 LLM Key（除非用户主动要求）
