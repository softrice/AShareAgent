---
description: AShareAgent 本地 A 股分析（仅用 Cursor 作 AI）
globs:
alwaysApply: true
---

# AShareAgent 工作流

本项目 **禁止** 调用 OpenAI / DeepSeek / 硅基流动等外部大模型 API。  
唯一 AI 后端就是当前 Cursor Agent。

## 用户说「分析 XXXXXX」或「分析某股票」时

1. 在项目根执行（优先本仓库 Python，不要依赖 TradingAgents-CN 路径）：
   - Windows: `analyze.bat <代码>`
   - 或: `python -m ashare_agent prepare <代码>`
2. 读取 `data/<代码>_context.md`（完整字段也在 `data/<代码>.json`）
3. 按 `ashare_agent/roles.py` 输出中文报告（技术面、大盘/行业、公告、资金流、股东/北向、多空辩论、目标价、decision JSON）
4. 用 `ashare_agent.fetch.save_report` 或 `python -m ashare_agent save <代码> --file ...`，或直接写入 `reports/<代码>_latest.md`
5. 报告末尾加免责声明：研究学习用途，不构成投资建议

## 报告结构（必须）

1. 市场/技术面分析师（引用 MA/MACD/RSI/布林/ATR/KDJ；尽量给支撑/阻力）
2. 中国市场分析师（大盘环境、行业语境、股东户数、北向/沪深港通、公告制度）
3. 基本面分析师
4. 新闻与公告分析师
5. 社媒情绪分析师
6. 多空辩论（看涨 / 看跌）
7. 风险官（激进 / 中性 / 保守视角）
8. 交易员与综合裁决（**目标价三情景 + decision JSON**）

## 目标价（对齐 TradingAgents-CN，强制）

- 保守 / 基准 / 乐观 三情景目标价（默认 1–3 个月）
- 合理区间（支撑–阻力）
- 止损或退出参考价
- 依据须引用数据包锚点；缺数据就标明

## 终裁 JSON（对齐 signal_processing，强制）

第 8 节末尾输出：

```json
{
  "action": "买入|持有|卖出",
  "target_price": 0,
  "target_price_conservative": 0,
  "target_price_optimistic": 0,
  "price_range": {"low": 0, "high": 0},
  "stop_loss": 0,
  "confidence": 0,
  "risk_score": 0,
  "time_horizon": "1-3个月",
  "reasoning": ""
}
```

- `action` 只能是买入/持有/卖出；研究上的「观望」请用「持有」并在 reasoning 说明
- 不构成投资建议

## 硬约束

- 不编造财报/新闻/公告/资金流/股东/北向；数据包没有的要写「数据缺失」
- A 股社媒只用雪球关注/东财股吧等公开指标
- 不要引导用户去部署 TradingAgents-CN 或申请第三方 LLM Key（除非用户主动要求）
